# PostgreSQL connection used by Python and dbt.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bike_share_etl
DB_USER=postgres
DB_PASSWORD=replace_with_a_private_password

# Host port mapped to PostgreSQL port 5432 inside the container.
DOCKER_DB_PORT=5433
