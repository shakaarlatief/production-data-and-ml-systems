name: production_data_and_ml_systems
version: "1.0.0"
config-version: 2

profile: production_data_and_ml_systems

model-paths: ["dbt/models"]
analysis-paths: ["dbt/analyses"]
test-paths: ["dbt/tests"]
seed-paths: ["dbt/seeds"]
macro-paths: ["dbt/macros"]
snapshot-paths: ["dbt/snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  production_data_and_ml_systems:
    +materialized: view
